When *Not* to Use Each System
=============================

.. todo::

   The flip side of "right tool for each stage": where each resource is the
   wrong choice, so participants can make deliberate trade-offs.

When Not to Use Cloud (Jetstream2)
----------------------------------

.. todo:: Workloads that outgrow interactive VMs or don't need a persistent host.

When Not to Use HPC (Anvil)
---------------------------

.. todo:: Embarrassingly parallel or bursty work that wastes a scheduled allocation.

When Not to Use HTC (OSPool)
----------------------------

.. todo:: Tightly-coupled, low-latency, or large-shared-state jobs that fight the HTC model.

Choosing Deliberately
---------------------

.. todo:: A short decision guide tying the trade-offs back to workflow stages.
