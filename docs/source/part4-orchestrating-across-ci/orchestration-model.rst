The Orchestration Model
=======================

.. todo::

   Introduce HTCondor + OSPool/PATh as an orchestration layer (a control plane)
   over the national CI, rather than a single place to run jobs. Frame how the
   Part 1–3 stages become one coordinated workflow.

Submit Locally, Run Everywhere
------------------------------

.. todo:: The access-point model — submit once, let HTCondor place work across pools.

Expressing the Pipeline as a Workflow
-------------------------------------

.. todo:: Chaining the preprocess → train → infer stages with dependencies (e.g. DAGMan).

PATh and the OSPool as a Control Plane
--------------------------------------

.. todo:: Where PATh fits, and how the pool abstracts over heterogeneous resources.
